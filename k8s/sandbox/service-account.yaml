apiVersion: v1
kind: ServiceAccount
metadata:
  name: openclaw-sandbox
  namespace: openclaw
  annotations:
    azure.workload.identity/client-id: "<sandbox_identity_client_id>"  # placeholder for terraform output
  labels:
    azure.workload.identity/use: "true"
