{
  "name": "openclaw-admin",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw Admin Panel — create agents via web UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
